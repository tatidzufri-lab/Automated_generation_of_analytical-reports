# Аналитический отчёт генератор

Python-проект для автоматической генерации аналитических отчётов в PDF и PPTX форматах на основе табличных данных (CSV или JSON).

## Возможности

- 📊 Анализ табличных данных (CSV/JSON)
- 📈 Автоматическое построение графиков (динамика продаж, топ позиций)
- 📄 Генерация PDF отчётов с красивым дизайном
- 🎯 Создание PowerPoint презентаций
- 🔧 Гибкая настройка через CLI параметры
- 📱 Адаптивный дизайн для печати и экрана

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

**Примечание для WeasyPrint:** На некоторых системах может потребоваться установка дополнительных системных библиотек:

- **Ubuntu/Debian:** `sudo apt-get install libcairo2-dev libpango1.0-dev`
- **macOS:** `brew install cairo pango`
- **Windows:** Следуйте инструкциям на [официальном сайте WeasyPrint](https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#installation)

## Использование

### Базовое использование

```bash
python report_cli.py -input data.csv -pdf output/report.pdf -title "Отчёт по продажам"
```

### Полный набор параметров

```bash
python report_cli.py \
  -input sample_data.csv \
  -datecol Date \
  -amountcol Amount \
  -pdf output/report.pdf \
  -pptx output/presentation.pptx \
  -title "Анализ продаж Q1 2024" \
  -topn 10
```

### Параметры CLI

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `-input` | Путь к входному файлу (CSV или JSON) | ✅ | - |
| `-datecol` | Имя колонки с датами | ❌ | - |
| `-amountcol` | Имя колонки с суммами | ❌ | - |
| `-pdf` | Путь для сохранения PDF | ❌* | - |
| `-pptx` | Путь для сохранения PPTX | ❌* | - |
| `-title` | Заголовок отчёта | ❌ | "Аналитический отчёт" |
| `-topn` | Количество позиций в топе | ❌ | 5 |

*Необходимо указать хотя бы один из форматов вывода (-pdf или -pptx)

## Структура проекта

```
project/
├── requirements.txt          # Зависимости Python
├── report_cli.py            # Основной CLI скрипт
├── data_types.py            # Работа с типами данных
├── analysis.py              # Анализ и построение графиков
├── build_pdf.py             # Генерация PDF
├── build_pptx.py            # Генерация PowerPoint
├── templates/
│   ├── report.html          # HTML шаблон для PDF
│   └── styles.css           # CSS стили
├── output/                  # Директория для выходных файлов
└── sample_data.csv          # Пример данных для тестирования
```

## Примеры данных

Проект включает примеры данных в разных форматах:

### CSV файл (sample_data.csv)
```csv
Date,item,Amount,Category
2024-01-15,Ноутбук Dell,45000,Электроника
2024-01-16,Мышь Logitech,2500,Аксессуары
...
```

### JSON файл (sample_data.json)
```json
[
  {"date": "2024-01-15", "product": "Ноутбук Dell", "amount": 45000, "category": "Электроника"},
  {"date": "2024-01-16", "product": "Мышь Logitech", "amount": 2500, "category": "Аксессуары"},
  ...
]
```

## Вычисляемые метрики

- **Общая сумма продаж** - сумма всех продаж
- **Средний чек** - среднее значение по колонке сумм
- **Количество заказов** - общее количество записей
- **Топ позиций** - N самых продаваемых товаров/категорий

## Графики

- **Динамика продаж** - линейный график по времени (автоматический выбор периода: дни/недели/месяцы)
- **Топ позиций** - горизонтальный барчарт с топ N позиций

## Форматы вывода

### PDF отчёт
- Красивый HTML-шаблон с адаптивным дизайном
- Встроенные графики и таблицы
- Оптимизирован для печати

### PowerPoint презентация
- Титульный слайд
- Слайд с ключевыми метриками
- Слайды с графиками
- Таблица топ позиций

## Обработка данных

- **Даты:** Автоматическое определение формата, удаление некорректных записей
- **Суммы:** Приведение к числовому формату, замена NaN на 0
- **Топ позиции:** Поиск колонки для группировки (item, product, name, category)

## Требования к системе

- Python 3.8+
- Зависимости из requirements.txt
- Для WeasyPrint: системные библиотеки Cairo и Pango

## Устранение неполадок

### Ошибки WeasyPrint
Если возникают ошибки при генерации PDF, убедитесь, что установлены системные зависимости:

```bash
# Ubuntu/Debian
sudo apt-get install libcairo2-dev libpango1.0-dev

# macOS
brew install cairo pango
```

### Проблемы с кодировкой
Убедитесь, что CSV файлы сохранены в кодировке UTF-8.

### Пустые графики
Если графики не отображаются, проверьте:
- Правильность указания колонок дат и сумм
- Наличие данных в указанных колонках
- Корректность формата дат

## Лицензия

MIT License
